import KiteStandardListingClient from "./KiteStandardListingClient";

export const metadata = {
  title: "Crear anuncio con peritaje | Quiver Admin",
};

export default function CrearAnuncioPage() {
  return <KiteStandardListingClient />;
}
